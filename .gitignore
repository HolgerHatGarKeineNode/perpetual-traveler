/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/vendor
.env
.env.backup
.env.production
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log

# Yarn Berry: nur die Release-Binary gehört ins Repo, sonst nichts
.yarn/*
!.yarn/releases
.pnp.*
/.fleet
/.idea
/.vscode
/.codegraph
/.playwright-mcp
*.png
# P4 (fullcalendar-7-umstieg): the four frozen v6 reference screenshots are
# tracked on purpose -- everything else under tests/Browser/ (in particular
# tests/Browser/Screenshots/, the ephemeral per-run output of every browser
# test) stays covered by the *.png rule above.
!/tests/Browser/References/*.png
plans/
